# tejitpabari.com Rewrite — Planning Folder

This folder plans the rewrite of `tejitpabari.com` from its current half-finished Gatsby 5 + Chakra UI scaffold to `vite-react-ssg` (React 19 + Vite) + Tailwind, prerendered to static HTML per route and deployed to Firebase Hosting. The goal is to reposition Tejit as a health-tech builder (Juno, an AI companion for medical appointments) rather than a geospatial engineer with side projects, and to give every project an individually linkable, individually shareable page with correct OG/share-preview metadata. `BRIEF.md` in this folder is the source of truth for every settled decision — every sub-project PRD below cites it directly and does not re-litigate anything it settles.

## Sub-projects

| Folder | Title | Scope | Depends on | Status |
|---|---|---|---|---|
| `01-app-shell-design-system-deploy` | App Shell, Design System & Deploy | Vite + `vite-react-ssg` + TypeScript toolchain; Tailwind design tokens ported from Brittne Valdivia's techfolio (palette, radii, shadows, Montserrat); the full route skeleton (`routes.tsx`, `getStaticPaths` seam); shared components (`Nav`, `Footer`, `PageShell`, `Button`, `TagPill`, `BackButton`, icon set, `useDebouncedValue`); `src/config/links.ts` (`RESUME_URL`, `NAV_LINKS`, `FOOTER_LINKS`); Firebase Hosting config. | None (first sub-project) | ✅ Complete (26/26) — reviewed as part of the final whole-project review, PASS |
| `02-content-pipeline` | Content Pipeline | Four content collections (Projects, Research, Work Experience, Legal) — on-disk layout, frontmatter contracts, `import.meta.glob` + `gray-matter` loaders, hand-rolled build-time validation (including the Nav/Footer href cross-check), `src/config/featured.ts`, the `DRAFT_DATE` placeholder mechanism, and the `/projects/<slug>/live` redirect-vs-hosted signal. | SP01 | ✅ Complete (15/15) — reviewed as part of the final whole-project review, PASS |
| `03-landing-page-timeline` | Landing Page & Work-Experience Timeline | Hero, Featured Projects, work-experience timeline (gbose-style border-left spine), About, and Contact sections on `/`; the `/work-experience` page; `ProjectCard` (shared verbatim with SP04). Consumes SP01's `src/config/links.ts` and SP05's `src/config/contact.ts`; creates neither. | SP01, SP02 | ✅ Complete (16/16) — reviewed, PASS |
| `04-projects-research-pages` | Projects & Research Pages | `/projects` and `/research` listing pages (shared search + tag-filter hook, Fuse.js); one data-driven detail template per collection; the resolved `/projects/<slug>/live` dual-mode contract (real HTTP redirect vs. hosted mini-project registry); `scripts/check-no-forms.sh`. | SP01, SP02, SP03 | ✅ Complete (25/25) — reviewed, remediated |
| `05-legal-analytics` | Legal Pages & Analytics | Obfuscated-email contact pattern; `ConsentContext` + consent banner; GA4 (`trackEvent`, the five-event `AnalyticsEventName` catalogue); `/privacy` and `/terms` drafted copy. | SP01 | ✅ Complete (20/20) — reviewed as part of the final whole-project review, PASS |
| `06-sharing-seo-sample-project` | Sharing, SEO & Sample Project | `RouteMeta` per-route OG/Twitter meta; build-time OG card generation; sitemap/robots; the deletable `sample-project` demo. | SP01, SP02, SP04 | ✅ Complete (18/18) — reviewed, remediated |
| `07-content-migration-copy` | Content Migration & Copy | Migration plan for all 17 content items (10 projects, 5 research, 2 work-experience roles) from the old Gatsby site into SP02's frontmatter contract, with per-item date-confidence tiering; drafted first-pass copy for hero, About, Contact, project/research descriptions, and work-experience blurbs. | SP02 | ✅ Complete (7/7) — reviewed, remediated |
| `08-ci-deploy-pipeline` | CI & Deploy Pipeline | Two hand-authored GitHub Actions workflows (PR-preview channel, merge-to-`main` live deploy) running SP02's/SP04's mechanical content gates plus a typecheck before every build; dedicated GCP service-account auth; `VITE_GA_MEASUREMENT_ID` as a repository variable with a fail-loud production guard; fork-PR guard; concurrency control; 7-day preview-channel expiry. | SP01, SP02, SP04, SP06 | ✅ On-disk work complete (Tasks 1–3, 25% by task count), reviewed PASS — merged into `website-revamp`; Tasks 4–8 (service-account/secret/variable provisioning, validation PR, production cutover) are owner-only |

## Dependency graph / phase ordering

This is the build order downstream implementation tooling (`dev-tasks`/`dev-code`) reads:

- **Phase 1:** SP01 (blocks everything)
- **Phase 2:** SP02 and SP05 in parallel (both depend only on SP01; disjoint files)
- **Phase 3:** SP07 and SP03 in parallel (SP07 authors `src/content/**`; SP03 builds landing + timeline components — disjoint files)
- **Phase 4:** SP04 (depends on SP03's `ProjectCard`)
- **Phase 5:** SP06 (depends on SP04's `/live` registry convention)
- **Phase 6 (last):** SP08 (depends on SP01, SP02, SP04, SP06) — every npm script its two CI workflows invoke (`typecheck`, `check:no-forms`, `check:launch`, `prebuild`, `build`) must already exist before its design does anything useful, which is exactly why it can't build sooner than everything else.

**Cross-dependency found during SP04+SP06's combined review (`04-projects-research-pages/review-2026-08-31-0900.md`, Architect persona):** the phase ordering above shows SP04 → SP06 one-directionally, but SP04's own detail pages (`ProjectDetailPage`, `ResearchDetailPage`) import SP06-owned `RouteMeta` directly — SP04's Tasks 9/10 bind to `RouteMeta`'s `{ title, description, path, image? }` signature and cannot typecheck without it. This forward dependency is why SP06 Tasks 1–2 (`src/config/site.ts`, `src/components/RouteMeta.tsx`) were pulled out of phase order and landed as `sp06-pre` before SP04's detail-page tasks, rather than waiting for SP06 proper to start in Phase 5. The two sub-projects' real build order was therefore SP04-minus-detail-pages → SP06 Tasks 1–2 → SP04 detail pages (+ the rest of SP04) → SP06 Tasks 3–18, not the strictly sequential Phase 4 → Phase 5 the graph implies.

## Status

**The initiative is finished.** All eight PRDs are settled, all eight sub-projects are implemented
and merged into `website-revamp`, and a final whole-project review has run across all four
reviewer passes (Adversarial Correctness, Deep Security across 11 categories, Test Coverage, and
a rollup of every per-sub-project review) — verdict **PASS after remediation**. The two build
breaks that the review's Adversarial Correctness pass found (both in the mandatory pre-launch
sample-project deletion step) are fixed with regression fixtures, and the post-deletion launch
rehearsal — actually deleting the sample project in a scratch copy and running the full toolchain
against it — is fully green. See **`review-2026-08-31-1000.md`** in this folder for the complete
findings, the real verification numbers, and every open non-blocking note. As of 2026-08-31:

- **SP01 (App Shell, Design System & Deploy) — complete.** All 26 tasks done, including two corrective fix commits caught by Task 26's build/lint/typecheck verification (a `react-hooks/set-state-in-effect` defect in `Nav.tsx`, and a no-op `typecheck` gate). See `01-app-shell-design-system-deploy/TASKS.md`.
- **SP02 (Content Pipeline) — complete.** Tasks 1–10 (collections, loaders, validation, the pre-launch content gate) plus Tasks 11–15 (validator/unit test suites) all done — 15/15. See `02-content-pipeline/TASKS.md`.
- **SP03 (Landing Page & Work-Experience Timeline) — complete.** All 16 tasks done (Hero, Featured Projects, work-experience timeline, About, Contact, `/work-experience` page, and the `ProjectCard` seam for SP04, plus their unit/integration tests). Reviewed 2026-08-31 (Bug Hunter, Security, Architect personas) — PASS, no must-fix items. See `03-landing-page-timeline/TASKS.md` and `03-landing-page-timeline/review-2026-08-31-0600.md`.
- **SP04 (Projects & Research Pages) — complete.** All 25 tasks done (shared search/filter hook, `/projects` and `/research` listing pages, data-driven detail templates, the `/live` dual-mode contract, `scripts/check-no-forms.sh`). Reviewed 2026-08-31 together with SP06 Tasks 1–12 (Bug Hunter, Security, Architect personas) — NEEDS_CHANGES at review, all findings since remediated on `website-revamp` (registry-derived sitemap slugs, `liveUrl` scheme validation, OG-card slug traversal, `check-no-forms.sh` regex bypass, sitemap XML-escaping, stale comments). See `04-projects-research-pages/TASKS.md`, `code-2026-08-31-0700.md`, and `review-2026-08-31-0900.md`.
- **SP05 (Legal Pages & Analytics) — complete.** Tasks 1–19 done (contact obfuscation, consent system, GA4 analytics, `/privacy` and `/terms` pages, and their unit/smoke tests); Task 20 (the `dist/` build audit) ran with no concurrent agents in the repo and all five acceptance checks passed against a real `npm run build` — 20/20. See `05-legal-analytics/TASKS.md`.
- **SP06 (Sharing, SEO & Sample Project) — complete.** All 18 tasks done (`RouteMeta`, `absoluteUrl`, vendored Montserrat fonts, build-time OG-card and sitemap generation, the `sample-project` demo, and their unit tests). Tasks 1–2 were pulled forward ahead of phase order (branch `sp06-pre`) to unblock SP04's detail pages, which import `RouteMeta` directly. Reviewed 2026-08-31 alongside SP04 — NEEDS_CHANGES at review, remediated except a still-open recommendation on the vendored fonts' provenance. See `06-sharing-seo-sample-project/TASKS.md`, `code-2026-08-31-0900.md`, and `review-2026-08-31-0900.md`.
- **SP07 (Content Migration & Copy) — complete.** All 7 tasks done (all 17 content items migrated, plus landing copy handoff and full-set validation). Reviewed 2026-08-31 with findings corrected in a follow-up commit. See `07-content-migration-copy/TASKS.md` and `07-content-migration-copy/review-2026-08-31-0526.md`.
- **SP08 (CI & Deploy Pipeline) — complete on disk, merged into `website-revamp`.** Branch `sp08-b1` merged (Tasks 1–3: package.json reconciliation, both GitHub Actions workflow files). Reviewed 2026-08-31 — PASS, no must-fix (`08-ci-deploy-pipeline/review-2026-08-31-0930.md`). Task 4 partial (`.gitignore` hardening done; the six-command GCP service-account provisioning sequence itself not run — owner-only, needs real `gcloud`/`gh` access). Tasks 5–8 are owner-only and not attempted by any implementation pass: setting the `VITE_GA_MEASUREMENT_ID` repository variable, opening a real validation PR, and the production-cutover merge to `main`.
- **`coverage-gaps` (test-coverage remediation, not a numbered sub-project) — complete, merged into `website-revamp`.** Closed six real coverage gaps found by the Test Coverage reviewer pass (OG-card PNG rendering, `parseWorkExperience`, validator throw paths, `parseResearch` throw paths, listing-page composition, duplicate-slug guards) with real assertions — no production defect found. Merge commit `98330ef`, clean, no conflicts.

Full per-sub-project implementation-run detail (task-by-task commit mapping, deviations, follow-ups): see `code-2026-08-31-0420.md` (SP01/SP02/SP03/SP05 baseline), `04-projects-research-pages/code-2026-08-31-0700.md` (SP04), `06-sharing-seo-sample-project/code-2026-08-31-0900.md` (SP06), and `08-ci-deploy-pipeline/code-2026-08-31-0930.md` (SP08) in this folder and its sub-folders. For the finished initiative's full final-state audit, see **`review-2026-08-31-1000.md`**.

## Still requires the owner

These are the items no agent can resolve — tracked in each PRD's own `§9`/`§8` and in the final
review's Deep Security section, aggregated here for one-glance visibility. Firebase project
creation and the DNS cutover, previously the top items here, are now **done**; SP08's on-disk
work (both GitHub Actions workflows) is also **done and merged** — everything below is either a
real-world action only someone with account/repo access can take, or a read-and-approve step
only the owner can make.

- **SP08 — Provision `FIREBASE_SERVICE_ACCOUNT_TEJITPABARI_99` and set `VITE_GA_MEASUREMENT_ID`, then cut over to `main`.** Three linked steps, in order: (1) run the six-command GCP service-account provisioning sequence (or `firebase init hosting:github` interactively) to create a dedicated service account (`roles/firebasehosting.admin` + `roles/firebase.viewer` only), key it, and upload it as a GitHub repository secret — SP08 §4.5 gives the exact commands; not launch-blocking by itself (the site still builds and can be deployed by hand without it) but blocks CI from deploying at all until done. (2) Run `gh variable set VITE_GA_MEASUREMENT_ID --body "G-9NLS3NG63M"` — SP08 §4.6; the merge workflow fails loudly if it's missing, so this can't ship silently, but someone still has to set it once. (3) Open a real validation PR (SP08 Task 7) to exercise the PR-preview workflow, then read PRD §4.4's operational-consequence framing and merge `website-revamp` into `main` (SP08 Task 8) — this deploys straight to the live channel with no separate review step, intentionally overwriting the current hand-deployed holding page.
- **Delete the `sample-project` demo before that same launch, once share-preview testing is confirmed on the real domain.** All four items: `src/content/projects/sample-project.md`, `src/pages/live/sample-project.tsx`, its `HOSTED_LIVE_PAGES` registry entry in `src/pages/live/registry.ts`, and `src/pages/live/sample-project.test.tsx` — SP06 PRD §8. This exact sequence was rehearsed end-to-end by the final review (see `review-2026-08-31-1000.md`'s Job B post-deletion rehearsal) and is now fully green (`npm run format`, `typecheck`, `check:launch`, `check:no-forms`, `build` all pass) — the two build breaks that rehearsal originally found are fixed (`fa36cee`, `7df4b7f`). Until this is actually done, `npm run check:launch`'s pre-launch content gate stays red on purpose (`sample-project.md` still has `demo: true`).
- **Read every word of the drafted `/privacy` and `/terms` copy and edit or approve it** (SP05), including the "This isn't professional or medical advice" framing on `/terms`. Not lawyer-reviewed; must not ship unread. Implemented in code, not just drafted in the PRD (`src/pages/PrivacyPage.tsx`, `src/pages/TermsPage.tsx`, reproduced verbatim from the PRD) — the review gap is the same one, but it now blocks real shipped pages, not just a document.
- **Confirm or correct the four low-confidence project dates** (SP07: Juno, Med-Doc Tracker, Crunchy Filler, Clip-Verse). A wrong guess silently reorders `/projects` and changes what `featured.ts` backfills, with no build-time signal. (Also open, lower priority: decide whether Med-Doc Tracker or any other body-less project should get a short `body` — default is no body, the site is complete either way.)
- **Confirm or replace the vendored Montserrat font provenance** (SP06). `scripts/assets/fonts/Montserrat-{Regular,SemiBold,Bold}.ttf` (satori's build-time OG-card renderer) have no committed checksum or license file, and were sourced via the third-party `gwfh.mranftl.com` (google-webfonts-helper) mirror rather than fetched directly from Google Fonts. Low risk (build-time only, never served to visitors, parsed by memory-safe Rust `satori`/`@resvg/resvg-js`), but worth a provenance record before treating it as permanently settled.
- **Security hardening notes from the final Deep Security review** (all non-blocking, none Critical/High): add response headers to `firebase.json` (`Content-Security-Policy`, `X-Content-Type-Options`, `Referrer-Policy`, HSTS) — none configured today; SHA-pin `FirebaseExtended/action-hosting-deploy@v0` (the one CI step that receives the deploy credential) instead of the floating major-version tag it uses now. See `review-2026-08-31-1000.md`'s Deep Security section for the full writeup, including two `react-router` advisories with no upstream fix, verified unreachable in this app's actual usage.
