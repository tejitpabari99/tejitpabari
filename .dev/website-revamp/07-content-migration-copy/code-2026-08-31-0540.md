# Code Run — 2026-08-31 05:40 UTC — SP07 review remediation

Branch: `sp07-remediate`   Base: `website-revamp` at `a6fe57a`

## Scope completed

This follow-up addresses only the two accepted SP07 findings recorded in the prior review. SP07 task completion counts were not changed.

| Finding | Status | Implementation commit | Files |
|---|---|---|---|
| Juno Website link used plaintext HTTP | done | `fcb3105` | `src/content/projects/juno.md` |
| Research abstracts were duplicated in `description` and markdown `body` | done | `fcb3105` | Five files under `src/content/research/` |
| Regression coverage for the corrected contracts | done | `fcb3105` | `src/data/projects.test.ts`, `src/data/research.test.ts` |
| SP07 design/task contract reconciliation | done | documentation commit in this branch | SP07 `PRD.md`, `TASKS.md` |

## Implementation details

- Juno's Website link is now `https://meetjuno.health/` in content and SP07 task/design references.
- All five research entries retain their non-empty abstract in frontmatter `description` and have an empty markdown body. This matches SP04's sequential `description` then `ContentBody body` rendering contract without changing SP04.
- Permanent tests prove `parseResearch` accepts an empty body, the loaded five-entry research corpus has empty bodies, and the loaded Juno Website link is HTTPS.

## Verification

- `npm test` — exit 0; 17 test files, 74 tests passed.
- `npm run check:launch` — exit 0; 1 test file, 5 tests passed.
- `npm run typecheck` — exit 0.
- `npm run lint` — exit 0.
- `npm run build` — exit 0; static generation completed.
- `git diff --check` — exit 0.

The build still emits pre-existing non-blocking warnings for Vite's future native config loader, the bundled `gray-matter` direct `eval`, and a client chunk over 500 kB.

## Deviations

- No SP07 task status or completion count was changed.
- No SP04 source, suppression logic, or unrelated content was changed.
- The branch remains unmerged; the worktree is intended for handoff to the parent branch.

## Still requires you

- Merge or cherry-pick the two commits from `sp07-remediate` into `website-revamp` if these changes are accepted.
- The previously documented SP07 owner follow-ups remain: edit/approve drafted copy, confirm low-confidence dates and live URL choices, manually check external-link liveness, and decide whether any body-less project needs a longer writeup.
