# Review — 2026-09-02 01:46 UTC — 04-component-polish

Commits: 80168ca, d8e5809, afe4c90, e296ce6, 955ecb9, 708b3b7, 0ed337f, 6fb27b1, 3e4f2fe, 788ff2d, cd7a28c, 088d53a
Personas: Bug Hunter, Architect, Accessibility & Visual Contract

## Verdict: PASS

## Must-fix

(none)

## Notes (non-blocking)

- [Bug Hunter] `src/components/StatusBadge.tsx` — the `satisfies Record<BadgeStatus, string>` exhaustiveness check plus content-parse-time `assertOptionalStatus` do jointly make an unmapped status unreachable, as the PRD claims. Confirmed against real content (`command grep -rn "^status:" src/content/`): every real file uses only `Building` or `Completed`; `Not Started` appears in zero content but is still styled (`bg-slate-dark/92`). No status value falls through unstyled.
- [Bug Hunter] `tailwind.config.ts` typography block — verified against the actual installed `mdast-util-to-hast` source (`node_modules/mdast-util-to-hast/lib/handlers/{list,list-item}.js`): a checked/unchecked list item really does get `className: ['task-list-item']` with a checkbox `<input>` unshifted as its first child, and the parent `<ul>` really does get `className: ['contains-task-list']` the moment any child is a task item. The three new CSS selectors (`ul.contains-task-list`, `li.task-list-item`, `li.task-list-item input[type="checkbox"]`) target the markup that is actually emitted, not a hypothesized one. Ran the real test suite (`npx vitest run` across all 8 R4-touched test files, 45/45 green) including the GFM fixture test, which asserts `li.task-list-item` count === 2 and `ul.contains-task-list` count === 1 against real rendered DOM — confirms the fix, not just the selector names.
- [Bug Hunter] `src/data/markdownComponents.tsx` — `isExternalUrl` import removed cleanly; grepped all remaining consumers (`src/data/index.ts`, `src/layout/Footer.tsx`, its own test) and confirmed none broke. No internal markdown link exists in any real content file today (verified), so the collapsed always-new-tab branch is currently invisible in production but is exercised correctly by the new test (`[Projects page](/projects)` renders `target="_blank" rel="noreferrer"`).
- [Bug Hunter] `LiveRedirectFallback`'s `backTo?: string` — confirmed `<BackButton to={backTo} />` degrades correctly: `BackButton`'s own signature is `{ to = '/', className = '' }`, and passing `to={undefined}` fires the default-parameter value identically to omission. Test suite pins both branches (`backTo` provided → href `/projects/juno`; omitted → href `/`), both pass.
- [Architect] Locked decision 12 respected precisely: `src/pages/ProjectLivePage.tsx` diff is exactly the one reassigned line (redirect-mode `backTo={...}`) per `TASKS.md`'s explicit orchestrator reassignment note; the hosted-mode branch's own pre-existing `<BackButton to={`/projects/${project.slug}`} />` (R1's) is untouched, confirmed by reading the full current file.
- [Architect] Locked decision 21 respected: `Nav.tsx` and `Footer.tsx` were not touched by any R4 commit (confirmed — no R4 commit lists these files) and both still route internal links via same-tab react-router `<Link>`; `Footer`'s one external link (techfolio attribution) stays `target="_blank"`, unaffected. `ProjectCard`'s own grid-navigation link (`<Link to={href}>` wrapping the title, with the `after:absolute after:inset-0` full-card click target) also correctly stayed same-tab — not converted to `target="_blank"`.
- [Architect] No duplication reintroduced: the old hardcoded pill markup (`rounded-full bg-teal/92 ...`) is fully removed from both `DetailHeader.tsx` and `ProjectCard.tsx`; both now consume the single shared `StatusBadge`. No scope creep — every commit touches only files R4 owns (plus the one reassigned `ProjectLivePage.tsx` line, which is explicitly sanctioned).
- [Accessibility & Visual Contract] Recomputed WCAG 2.x contrast independently (relative luminance on linearized sRGB, white text) rather than trusting the PRD's numbers: `teal` #043439 → 13.50:1 opaque / 10.67:1 worst-case; `status-building` #92400E → 7.09:1 / 5.94:1; `slate-dark` #4D5D59 → 6.94:1 / 5.68:1. All three clear 4.5:1 by wide margins even under the pessimistic 92%-opacity-blended-with-white bound; the PRD's own figures (13.50/10.71, 7.09/5.94, 6.94/5.67) match to within rounding-method noise (<0.1). No contrast problem found.
- [Accessibility & Visual Contract] Status is conveyed non-visually, not by color alone: `StatusBadge` renders the status word itself (`Building`/`Completed`/`Not Started`) as the badge's text content in every case, satisfying "don't rely on color alone."
- [Accessibility & Visual Contract] Every new/changed `target="_blank"` link (`markdownComponents.tsx`'s `a()`, `LinksRow.tsx`'s "Open Live" CTA) carries `rel="noreferrer"`, consistent with every pre-existing `target="_blank"` link in the codebase.

Owner-only items already known and correctly not re-reported: eyeballing the three badge colors on real photos; confirming the new-tab scope carve-out matches intent; `firebase.json` build-dirtiness (R6); the Task 10 stale "7 cases" count in `TASKS.md` prose.

## Next step

No blocking or must-fix issues found across all three persona passes. R4 (Tasks 1–12) is ready to proceed to the next phase per the round's dependency graph (R5, then R6's final sweep/gate/ship). Owner-only follow-ups (badge-color eyeball, new-tab scope confirmation) remain open per PRD §8 and the round README's "Still requires the owner" table — unchanged by this review, not blocking.
