# Review — 2026-09-03 01:15 UTC — 02-landing-sections

Commits: 8f90fa0, 6bf23b6, 43bd3ca, ee03ddd, 76c94d5, dc59472, 9a33849, 6b28d96
Personas: Bugs/Correctness, Copy Accuracy & Round Rules, Architecture Fit

## Verdict: PASS

## Must-fix

(none)

## Notes (non-blocking)

- [Bugs/Correctness] `dc59472` ("Fix Featured Projects headline wrap at 375px") is a real, well-reasoned
  fix — it drops the shared below-`sm` headline base size from `1.7rem` to `1.15rem` because, worked
  through the PRD's own glyph-width estimate, `1.7rem` at a 327px available width (375px viewport minus
  `2×px-6`) yields roughly 20–21 characters per line against a 41-character string, right at the edge of
  producing a 3-line wrap instead of the intended 2. The arithmetic is internally consistent with the
  PRD's own §4.7 methodology and the commit message shows the work. However, this fix landed as its own
  ad hoc commit between Task 5 and Task 6 and is **not tied to any task number or acceptance criterion in
  TASKS.md** — it's only referenced retroactively, in passing, inside Task 6's completion note ("the
  3-line-wrap regression `dc59472` fixed"). No task file entry documents that this bug existed, was
  found, or was fixed. This is the same category of gap the sibling R1 review flagged (an undocumented,
  but correct, deviation) — worth a note, not a blocker, since the change stayed inside R2's owned files
  and the reasoning is sound.

- [Bugs/Correctness] The `9a33849` mobile-headline-size arithmetic (`1.15rem → 1.5rem`) checked out under
  independent recomputation: at a 375px viewport (327px available), required box width at `1.5rem` is
  `22 × 0.58em × 1.5rem × 16px/rem ≈ 306px`, which stays under 327px, so the `max-w-[22ch]` box remains
  unclamped and the string wraps at ~22 chars/line as designed (→ 2 lines for both 41- and 38-character
  headlines). The claimed "~1.6rem threshold" is correct (327 / (22×0.58×16) ≈ 1.60rem). One thing not
  called out in the commit: this margin was verified only down to 375px, matching Task 6's actual
  acceptance criteria — the PRD explicitly disclaims a guarantee below that (§3 non-goal), so this is
  consistent, not a gap. At a narrower phone (320px, e.g. iPhone SE), the same math puts required chars
  at ~19–20/line, which is tighter and could tip into a 3rd line depending on word-break points — outside
  what this sub-project promises or was asked to check, flagged only for completeness.

- [Bugs/Correctness] `9a33849`'s commit message states "45 files / 254 tests" but a plain file count of
  `src/**/*.test.*` in the current tree returns 44. This is very likely explained by concurrent sibling
  sub-project activity in this shared working tree (the tree currently also has an unrelated uncommitted
  modification to `src/content/projects/juno.md` from another in-flight agent) rather than any defect in
  R2's own commits — R2 added exactly one test (`WorkExperienceSection (real content)`) inside an
  existing file, not a new file, so R2's own commits don't change the file count. Noted only as a
  plausibility check that didn't land exactly on the number reported; not attributable to R2.

- [Architecture Fit] The `dc59472`/`9a33849` mobile-size changes leave `WorkExperienceSection`'s and
  `FeaturedProjectsSection`'s below-`sm` headline size (`1.5rem`) still smaller than `About`'s heading
  (`1.75rem`) and `Contact`'s heading (`1.75rem`) at the same breakpoint — the `9a33849` commit message
  frames `1.5rem` as "a much closer visual match" to those, which is accurate but the sizes are still not
  unified. This is a design/taste question the PRD never actually asked this sub-project to solve (it
  only asked for a one-line guarantee ≥640px and a clean 2-line wrap below it, both satisfied) — flagged
  only as a possible follow-up for a future polish pass, not a defect against this PRD's actual scope.

## Copy accuracy & round rules — verification detail

- **Em dashes:** `grep -rP '&mdash;|\x{2014}'` across `src/sections/*.tsx`/`*.ts` returns hits only inside
  code comments (`HeroPortrait.tsx`'s pre-existing docstring, `WorkExperienceSection.tsx`'s/`.test.ts`'s
  and `ContactSection.test.tsx`'s explanatory comments, and `Hero.tsx`'s JSX comment) — zero em dashes in
  any user-visible string. Confirmed directly against the live file contents, not just the diffs.
- **Hiring statement (locked decision 8):** `grep -in 'hiring\|looking for work\|open to
opportunities\|available for hire'` across `src/sections/*.tsx` returns zero matches. The old
  `ContactSection.tsx` paragraph ("Whether you're hiring...") is fully gone; the new paragraph is about
  health tech and reachability only, in neither direction.
- **Hero eyebrow (locked decision 10):** `Hero.tsx`'s eyebrow `<p>` is byte-for-byte unchanged — "Health
  Tech Builder" — confirmed by reading the current file; only the paragraph below it changed, exactly as
  the PRD's before/after diff specifies.
- **Factual claims, cross-checked against `RESUME-EXTRACT.md`:** "Software Engineer II on the Fabric Maps
  team" — matches (Level 61, March 2024–Present). "geospatial data gets visualized and analyzed in
  Microsoft Fabric and Power BI" — accurate paraphrase of the resume's own team-context line ("Fabric Maps
  enables visualization and analysis of geographical data in Microsoft Fabric and PowerBI"). "a
  self-testing app for HIV and syphilis" — matches the SMARTest research-paper title. "a tool that
  identifies pills from photos" — sourced from the pre-existing, out-of-scope
  `src/content/research/pill-recognition-prescription-extraction.md`, reworded only, claim not
  strengthened. No invented biography found anywhere in the four rewritten copy blocks (Hero paragraph,
  About's four paragraphs, Contact's paragraph).
- **Voice ("reads like a person"):** all four rewritten blocks use short declarative sentences, no
  "not X, but Y", no tricolon lists, no "passionate about", no sentence-initial "Whether" (the old
  Contact paragraph's "Whether you're hiring..." construction is gone).

## Architecture fit — verification detail

- **R1 `PageContainer` parity (README decisions 13/14):** `src/layout/PageContainer.tsx` renders
  `` `mx-auto w-full max-w-content px-6 pb-20 ${TOP_PADDING} sm:px-8 md:px-10 lg:px-12` `` on its own
  wrapper element. All four R2 sections apply the padding ramp (`px-6 ... sm:px-8 md:px-10 lg:px-12`) on
  their outer `<section>` and the `mx-auto w-full max-w-content` bound one level in, on the content
  `<div>` — a different DOM split (section owns padding vs. `PageContainer` owning both padding and
  width on one element) but the identical class values, which is exactly what the PRD's header note and
  README decision 13 claim: matched convention, not shared code, because landing sections are full-bleed
  colored bands that can't adopt `PageContainer` as their own outer element. Genuinely consistent, not
  drift.
- **R3 data integration:** `src/content/work-experience/` holds exactly 3 files
  (`microsoft-fabric-maps-swe-ii.md`, `microsoft-fabric-maps-swe.md`, `jio-reliance-industries.md`), so
  `workExperience.length === 3 > LANDING_TIMELINE_LIMIT (2)` — confirmed true, and the new
  `WorkExperienceSection.test.ts` test **actually renders** `<WorkExperienceSection>` (not a mock) and
  asserts `getAllByRole('listitem')` has length 3 (2 real entries + 1 "See all experience" stub) and that
  the stub links to `/work-experience`. This is a real, previously-never-exercised code path (the stub
  had only ever been reasoned about, never rendered) and the test is a genuine behavioral assertion, not
  one that would pass regardless of the underlying component.
- **`FEATURED_PROJECT_SLUGS` integration:** `FeaturedProjectsSection.tsx` still consumes
  `featuredProjects` from `@/config/featured` with zero changes to the data-consumption code — confirmed
  by reading the current file; only the headline/width JSX changed, as the PRD requires.
- **File boundary:** `git show --stat` across all eight R2 commits shows changes touching only
  `src/sections/Hero.tsx`, `AboutSection.tsx`, `ContactSection.tsx`+`.test.tsx`,
  `FeaturedProjectsSection.tsx`, `WorkExperienceSection.tsx`+`.test.ts`, and (one commit, `6b28d96`)
  `.dev/website-revamp-r2/02-landing-sections/TASKS.md` itself (a doc-only completion-note commit, in
  bounds for updating this sub-project's own task file). No file outside R2's declared ownership
  (`src/sections/*` + tests) was touched by any of these eight commits.

## Bugs/correctness — additional verification detail

- `ContactSection.test.tsx`'s current content matches Task 3's authoritative "after" block exactly
  (confirmed by diff): `EmailIcon` import removed, aside email block deleted, divider (`border-t
border-teal-secondary/10 pt-4`) removed since Profiles is now the aside's only block, and the test
  correctly drops to a length-1 assertion on `CONTACT_EMAIL_DISPLAY` instead of the old length-2
  assertion that would have broken.
- The two headline `<h2>` class strings in `FeaturedProjectsSection.tsx` (line 17) and
  `WorkExperienceSection.tsx` (line 35) are confirmed byte-for-byte identical in the current tree
  (`sm:max-w-none sm:whitespace-nowrap sm:text-[clamp(1.4rem,3.2vw,2.5rem)] sm:leading-[0.96]` tail and
  `max-w-[22ch] text-balance text-[1.5rem] font-extrabold leading-[1.1] tracking-tight text-ink` head),
  satisfying Task 5's own acceptance criterion 2 and the design rationale in PRD §4.7.
- `src/pages/HomePage.test.tsx`'s section-order assertion (`['', 'projects', 'work-experience', 'about',
'contact']`, 5 sections) is unaffected — none of R2's changes touch section `id`s or ordering, and the
  test file itself was not modified by R2. Confirmed still true against the current section files.

## Known, accepted limitation (not re-reported)

Real-browser viewport verification (Task 6: 640/768/1024/1440px one-line, 375px two-line wrap; Task 7:
≥1280px left-edge alignment) was not executed on this machine — no browser available, honestly recorded
as owner-only in both tasks' completion notes rather than claimed as passing. This matches the review
brief's known, already-accepted deferral and is not repeated here as a new finding.

## Next step

No must-fix items. R2 is complete within its own scope: width system matches R1's convention by design
(not code-shared, as intended), copy rewrites are voice-compliant and factually traceable, the hiring
statement is fully gone, the Hero eyebrow is untouched, and the newly-reachable "See all experience" path
is covered by a real rendered assertion rather than a trivially-passing one. The one item worth folding
into a future housekeeping pass (not blocking): retroactively note commit `dc59472` against a task number
in TASKS.md, since it's currently only referenced in passing inside Task 6's completion note rather than
documented as its own fix. Nothing further required from R2 before it can be considered done.
