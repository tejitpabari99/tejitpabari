# Review — 2026-09-02 01:42 UTC — 01-shell-nav-chrome

Commits: 58ba4cc, e835643, 5d5d5eb, 6542a95, fce298e, b7ea9a7, 8930318, d1c2e77, 95945a5,
dd67003, 8d7b507, 397f03d, f4622ba, 8d341e3, 0e56c67, 25198eb, dceb748, 94aae78, 40db8a3, ede1925
Personas: Bug Hunter, Architect, Consistency & Regression

## Verdict: PASS

## Must-fix

(none)

## Notes (non-blocking)

- [Bug Hunter] `src/layout/Nav.tsx:26-28` — the implementer added `window.scrollY > 0 &&` to the
  `nearPageBottom` guard, which is **not** in the PRD §4.3 / TASKS.md Task 3 authoritative "after"
  block (an undocumented deviation, not called out in the commit message). It is, however, a
  necessary and correct fix: without it, `document.documentElement.scrollHeight` being `0`/small
  relative to `window.innerHeight` (jsdom's default layout, and any real short-content-vs-tall-viewport
  page) makes `nearPageBottom` true on the very first call inside the mount effect, which would
  incorrectly mark the last section (`contact`) active instead of `Home` at page-load with zero
  scroll — and would have failed Task 3's own new test (`'highlights Home ... at the untouched top
of "/"'`) had it been omitted. Confirmed by running `src/layout/Nav.test.tsx` — all 3 cases pass
  with the guard in place. Correct fix, just should have been called out explicitly since it's a
  behavior change beyond what either authoritative code block specifies.

- [Consistency & Regression] TASKS.md Task 11 acceptance criterion 4 (`grep -rn "navigate(-1)"
src/components/BackButton.tsx` → no match) contradicts Task 11's own authoritative "after" code
  block, whose JSDoc comment contains the literal substring `` `navigate(-1)` `` ("Always a real
  `<Link>`, never `navigate(-1)`..."). The implementer (commit `8d7b507`) correctly copied the
  authoritative block verbatim, including that comment — the actual requirement (no `navigate(-1)`
  _call_ in the component logic) is satisfied; the file has no `navigate(-1)` invocation anywhere.
  The acceptance criterion's grep is simply wrong as written (it would fail against the very code
  block it's supposed to gate), not a defect in the shipped code.

- [Consistency & Regression] TASKS.md Task 19 acceptance criterion 4 (`grep -c 'lg:px-12'
src/pages/WorkExperiencePage.tsx` → `1`) contradicts Task 19's own authoritative "after" block,
  which routes the page through `<PageContainer chrome="full">` — abstracting `lg:px-12` into
  `PageContainer.tsx` (confirmed present there: `.../sm:px-8 md:px-10 lg:px-12 ...`) rather than
  leaving it as a literal substring in the page file. The implementer (commit `94aae78`) correctly
  followed the authoritative block; `WorkExperiencePage.tsx` itself contains no literal `lg:px-12`
  and does not need to — the shared component supplies it, satisfying the actual PRD §1 item 4
  requirement (the page now gets `lg:px-12`, previously missing) without duplicating the class
  string. No real requirement was missed; the acceptance criterion's grep target is stale.

- [Bug Hunter] `src/hooks/useCollectionFilter.ts` (touched by `ede1925`, not part of R1's own file
  ownership but authorized as a Task-21-gate fix) — a pre-existing, unrelated-to-this-commit URL
  round-trip artifact: on a direct/shared load of e.g. `/projects?q=foo&tag=bar`, the mount effect
  that adopts `q`/`tag` into state (line ~52) and the separate effect that writes `debouncedQuery`/
  `activeTag` back into the URL (line ~83) both fire in the same initial commit, with the second
  effect's closure still seeing the pre-adoption empty state — so the URL is briefly `replace`d to
  strip `?q=&tag=` before being restored (`tag` on the next render, `q` ~200ms later once the
  debounce catches up). This exists identically before and after `ede1925` (effect ordering and
  `useDebouncedValue`'s own initial-value semantics are unrelated to the `startTransition` wrapper);
  the fix under review neither introduces nor worsens it. It self-corrects with no `push`-history
  side effect (confirmed by the existing `'updates the URL via replace, not push'` test) and no
  incorrect final filter state. Flagging for awareness only — out of scope for this fix and for R1.

## Next step

No must-fix items. Nothing further required from R1 before it can be considered done. The one
genuine gap already known and correctly out of scope for any task file — `LiveRedirectFallback`'s
`backTo` prop (PRD §4.9/§9 item 6, R4's half of the split) — is not yet implemented anywhere in the
repo; confirmed R1's `ProjectLivePage.tsx` redirect-mode call site is unchanged (`<LiveRedirectFallback
to={project.liveUrl} label={project.title} />`, no `backTo`) exactly as Task 16 documents. This is
correctly R4's responsibility, not a gap in R1's own commits. Route to R4 (`04-component-polish`) to
confirm it lands `backTo` and updates this call site; no action needed from R1.
